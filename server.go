package main

import (
	"fmt"
	"ikascrew/server"
	"log"
	"net/http"
	"os"
	"runtime"
)

func init() {
	fmt.Println("########################## Starting ikascrew Server")
}

func main() {

	if err != nil {
		fmt.Println(err)
		os.Exit(1)
	}
	return
}
